#include <bits/stdc++.h>

#include "DCEL.hpp"

int solve(const std::vector<std::vector<std::string>> &,
          std::set<std::string> &, std::vector<bool> &);

void get_all_solutions(const std::vector<std::vector<std::string>> &,
                       std::set<std::string> &, std::vector<bool> &, int);
